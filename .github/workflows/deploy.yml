name: Deploy to Ubuntu Server

on:
  push:
    branches:
      - prod
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout latest code
      - name: Checkout code
        uses: actions/checkout@v3

      # 2. Install dependencies and build on GitHub runner
      - name: Install dependencies and build
        run: |
          npm install
          npm run build

      # 3. Upload only necessary files to the server
      - name: Upload build to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: ${{ secrets.SERVER_PORT || 22 }}
          source: |
            dist
            package.json
            package-lock.json
          target: /root/telegram-bot
          overwrite: true

      # 4. SSH into server and restart the bot
      - name: Deploy and restart bot
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: ${{ secrets.SERVER_PORT || 22 }}
          script: |
            cd /root/telegram-bot

            echo "📥 Installing production dependencies..."
            npm install --omit=dev

            echo "🔄 Restarting bot with PM2 using start:all..."
            pm2 restart telegram-bot || pm2 start npm --name "telegram-bot" -- run start:all

            echo "✅ Deployment completed!"
            pm2 status telegram-bot
